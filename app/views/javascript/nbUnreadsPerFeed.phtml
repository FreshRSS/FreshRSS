<?php
declare(strict_types=1);
/** @var ViewJavascript $this */

use FreshRss\Minz\Request;
use FreshRss\Minz\Translate;
use FreshRss\Models\Feed;
use FreshRss\Models\ViewJavascript;

$result = [
	'feeds' => [],
	'tags' => [],
];
$nbUnreadFeeds = 0;
foreach ($this->categories as $cat) {
	foreach ($cat->feeds() as $feed) {
		if ($feed->priority() > Feed::PRIORITY_HIDDEN) {
			$feedUnread = $feed->nbNotRead();
			$result['feeds'][$feed->id()] = $feedUnread;
			$nbUnreadFeeds += $feedUnread;
		}
	}
}
foreach ($this->tags as $tag) {
	$result['tags'][$tag->id()] = $tag->nbUnread();
}
$nbUnreadBefore = max(0, Request::paramInt('previous_unread') ?: 0);
$nbNew = max(0, $nbUnreadFeeds - $nbUnreadBefore);
$result['notifBody'] = trim(
	(Translate::plural('gen.js.feedback.body_new_articles', $nbNew) ?? _t('gen.js.feedback.body_new_articles', $nbNew)) . ' ' .
	(Translate::plural('gen.js.feedback.body_unread_articles', $nbUnreadFeeds) ?? _t('gen.js.feedback.body_unread_articles', $nbUnreadFeeds))
);
echo json_encode($result);
